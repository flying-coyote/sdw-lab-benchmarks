{
  "assumption_tested": "does the generic-path window-drop persist on an MPP OLAP engine (StarRocks)?",
  "benchmark": "ocsf-sigma-detection / StarRocks execution (H-SIGMA-01 lakehouse leg, backend 6 \u2014 GENERIC path)",
  "compile_path": "generic pySigma SQLite backend (no dedicated StarRocks backend), executed verbatim on StarRocks (MySQL dialect accepts alias-in-HAVING)",
  "emit_classification": "WINDOWLESS (timespan dropped)",
  "emitted_sql": "SELECT actor_user, COUNT(*) AS event_count FROM (SELECT * FROM logs WHERE outcome='FAILURE') AS subquery GROUP BY actor_user HAVING event_count >= 10",
  "event_count": {
    "correct_windowed_sql": "SELECT actor_user FROM (SELECT actor_user, count(*) c FROM sigma.logs WHERE outcome='FAILURE' GROUP BY actor_user, (`timestamp` DIV 600) HAVING count(*) >= 10) t GROUP BY actor_user",
    "emitted_run": {
      "flagged": 70,
      "fp_benign": 0,
      "fp_decoy": 50,
      "fp_total": 50,
      "precision": 0.286,
      "recall": 1.0,
      "tp": 20
    },
    "windowed_control": {
      "flagged": 20,
      "fp_benign": 0,
      "fp_decoy": 0,
      "fp_total": 0,
      "precision": 1.0,
      "recall": 1.0,
      "tp": 20
    }
  },
  "events": 4093,
  "evidence_tier": "B (single host; synthetic planted corpus; StarRocks + generic pySigma SQLite backend, no dedicated StarRocks backend)",
  "three_band": "SILENTLY-DEGRADES (windowless emit + over-fire despite window-function capability)"
}