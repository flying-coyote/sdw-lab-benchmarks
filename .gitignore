.venv/
__pycache__/
*.pyc
.DS_Store
# generated corpora — large, reproducible from the generators, not checked in
_work/
*.parquet
.jars/
# transient bench run logs (regenerable; never a committed artifact). Supersedes the
# earlier scoped ocsf-mapping-oracle/results/*.log rule.
*.log
# tool-emitted OCSF output (large, regenerable from the pinned mapping) — keep scores, not the raw events
*.emitted.jsonl

# local agent notes (operate from project1; keep public repo minimized)
CLAUDE.md
