.venv/
__pycache__/
*.pyc
.DS_Store
# generated corpora — large, reproducible from the generators, not checked in
_work/
*.parquet
.jars/
ocsf-mapping-oracle/results/*.log
# tool-emitted OCSF output (large, regenerable from the pinned mapping) — keep scores, not the raw events
*.emitted.jsonl
